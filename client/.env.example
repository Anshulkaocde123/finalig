# =============================================================
#  VNIT Inter-Department Games — CLIENT Environment Variables
#  Copy this file to client/.env.local  (local dev)
#                      client/.env.production  (production build)
#  All variables MUST start with VITE_ to be exposed to the browser
#  DO NOT commit .env.local to version control
# =============================================================

# ─── API / Backend ───────────────────────────────────────────
# Full URL of the backend Express server (no trailing slash)
# Local dev:  http://localhost:5001/api
# Production: https://<your-backend-domain>/api
VITE_API_URL=http://localhost:5001/api

# ─── Socket.IO ───────────────────────────────────────────────
# Root URL of the backend server (same host, no /api path)
# Local dev:  http://localhost:5001
# Production: https://<your-backend-domain>
VITE_SOCKET_URL=http://localhost:5001

# ─── Google OAuth ────────────────────────────────────────────
# The same Client ID used in the server .env
# Found in: Google Cloud Console → APIs & Services → Credentials
VITE_GOOGLE_CLIENT_ID=xxxxxxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
